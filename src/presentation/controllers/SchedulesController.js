import { getSupabase } from '../../data/supabaseClient';
import { requireRole, _dbErr } from '../../shared/utils/apiHelpers';
import { SchedulesRepository } from '../../data/repositories/SchedulesRepository';

export class SchedulesController {
  static async handle(req, res, user) {

  }
}
